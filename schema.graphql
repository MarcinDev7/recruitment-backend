type Query {
  users: [User]
}

type User {
  id: ID!
  name: String!
  roles: [String]
}

type AddBookMutationResponse {
  code: String!
  success: Boolean!
  message: String!
  user: User
}

type Mutation {
  addUser(name: String): AddUserMutationResponse
}
